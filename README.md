# CV Assignment 2 – Proposal 6: Underwater Semantic Segmentation (SUIM)

Pipeline includes:
- Custom model UNet-ResAttn (UNet + Residual blocks + Attention gates)
- Training with/without augmentation
- Evaluation: mIoU + per-class IoU + parameter count
- Baselines: SUIM-Net (paper-inspired) + DeepLabV3-ResNet50

Open `notebook/UnderwaterSegmentation.ipynb` in VS Code and run top-to-bottom.
